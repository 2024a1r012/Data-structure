#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int row,i;
    scanf("%d",&row);
    int arr[row];
    for(i=0;i<row;i++){
        scanf("%d",&arr[i]);
    }
    for(i=0;i<row;i++){
        printf("%d ",arr[i]);
    }
    return 0;
}
