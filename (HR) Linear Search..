#include <stdio.h>

int main() {
    int n, t, f = 0;
    scanf("%d", &n);
    int a[n];
    for (int i = 0; i < n; i++) scanf("%d", &a[i]);
    scanf("%d", &t);
    for (int i = 0; i < n; i++)
        if (a[i] == t) {
            f = 1;
            break;
        }
    if (f) printf("Found\n");
    else printf("Not Found\n");
    return 0;
}
